import { AdminLogginsInterceptor } from './admin-loggins.interceptor';

describe('AdminLogginsInterceptor', () => {
  it('should be defined', () => {
    expect(new AdminLogginsInterceptor()).toBeDefined();
  });
});
